[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fabricagentdocumentextraction"
version = "0.1.0"

[tool.setuptools]
py-modules = ["VerifyAgent"]     # <-- exposes VerifyAgent.py as an importable module